[package]
name = "mypy-type-kernel"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
name = "type_kernel"
crate-type = ["cdylib", "rlib"]

[features]
extension-module = ["pyo3/extension-module"]

[dependencies]
pyo3 = { version = "0.20.2", features = ["abi3-py37"] }
